
// ---- Array 新方法 ----
export const corpus1 = [
  `return [3,1,2].toSorted();`,
  `return [3,1,2].toSorted((a,b)=>b-a);`,
  `return [3,1,2].toReversed();`,
  `return [1,2,3].toSpliced(1,1);`,
  `return [1,2,3].toSpliced(1,0,9);`,
  `return [1,2,3].with(1,9);`,
  `return [1,2,3,4].findLast(x=>x%2===0);`,
  `return [1,2,3,4].findLastIndex(x=>x%2===0);`,
  `return [1,2,3].at(1);`,
  `return [1,2,3].at(-1);`,
  `return [1,2,3].at(9);`,
  `return [[1,2],[3,4]].flat();`,
  `return [[1,2],[3,4]].flatMap(x=>x.map(y=>y*2));`,
  `return [1,2,3].reduce((a,b)=>a+b,0);`,
  `return [1,2,3].reduce((a,b)=>a+b);`,
  `return [1,2,3].reduceRight((a,b)=>a-b,0);`,
  `return [1,2,3,4].some(x=>x>3);`,
  `return [1,2,3,4].every(x=>x>0);`,
  `return [1,2,3].join("-");`,
  `return [1,2,3].toString();`,
  `return [1,[2,[3,[4]]]].flat(Infinity);`,
  `return [1,[2,[3,[4]]]].flat(2);`,
  `return [1,,3].flat();`,
];

// ---- Set 集合代数 ----
export const corpus2 = [
  `return new Set([1,2,3]).union(new Set([3,4]));`,
  `return new Set([1,2,3]).intersection(new Set([2,3,4]));`,
  `return new Set([1,2,3]).difference(new Set([2]));`,
  `return new Set([1,2]).symmetricDifference(new Set([2,3]));`,
  `return new Set([1,2]).isSubsetOf(new Set([1,2,3]));`,
  `return new Set([1,2,3]).isSupersetOf(new Set([1,2]));`,
  `return new Set([1]).isDisjointFrom(new Set([2]));`,
  `return new Set([1,2]).isDisjointFrom(new Set([2,3]));`,
  `return new Set([1,2,3]).size;`,
  `return [...new Set([1,2,1,3])];`,
  `return new Set([1,2,3]).has(2);`,
  `return new Set([1,2,3]).has(9);`,
  `return Array.from(new Set([1,2,3]));`,
];

// ---- Map 方法 ----
export const corpus3 = [
  `const m = new Map([["a",1],["b",2]]); return m.get("a");`,
  `const m = new Map([["a",1],["b",2]]); return m.get("z");`,
  `const m = new Map([["a",1]]); m.set("b",2); return m.get("b");`,
  `const m = new Map([["a",1]]); m.delete("a"); return m.size;`,
  `const m = new Map([["a",1],["b",2]]); return m.has("a");`,
  `const m = new Map([["a",1]]); return Array.from(m.keys());`,
  `const m = new Map([["a",1]]); return Array.from(m.values());`,
  `const m = new Map([["a",1]]); return Array.from(m.entries());`,
  `return new Map([["a",1],["b",2]]).size;`,
  `const m = new Map([["a",1]]); return m.forEach ? "fn" : "no";`,
  `const m = new Map(); m.set("k", undefined); return m.get("k");`,
  `const m = new Map(); m.set("k", undefined); return m.has("k");`,
];

// ---- BigInt ----
export const corpus4 = [
  `return 2n + 3n;`,
  `return 2n * 3n;`,
  `return 7n / 2n;`,
  `return 7n % 2n;`,
  `return 2n ** 3n;`,
  `return BigInt(5);`,
  `return BigInt("10");`,
  `return typeof 5n;`,
  `return 5n === 5n;`,
  `return 5n === 5;`,
  `return 5n == 5;`,
  `return String(5n);`,
  `return (5n).toString();`,
  `return BigInt.asIntN(8, 300n);`,
  `return BigInt.asUintN(8, 300n);`,
  `return 3n < 2n;`,
  `return 1n ? "t" : "f";`,
];

// ---- 全局转换函数 ----
export const corpus5 = [
  `return parseInt("42");`,
  `return parseInt("42px");`,
  `return parseInt("0x1F");`,
  `return parseInt("101", 2);`,
  `return parseInt("abc");`,
  `return parseFloat("3.14");`,
  `return parseFloat("3.14px");`,
  `return parseFloat("abc");`,
  `return Number("42");`,
  `return Number("");`,
  `return Number("  ");`,
  `return Number(null);`,
  `return Number(undefined);`,
  `return Number(true);`,
  `return isFinite(42);`,
  `return isFinite("42");`,
  `return isFinite(Infinity);`,
  `return isFinite(NaN);`,
  `return isNaN("abc");`,
  `return isNaN(42);`,
  `return globalThis.isFinite("42");`,
  `return Boolean("");`,
  `return Boolean("x");`,
  `return !!"";`,
  `return !!"x";`,
  `return !0;`,
  `return String(undefined);`,
  `return String(null);`,
  `return String(true);`,
  `return String([1,2]);`,
];

// ---- 编码 ----
export const corpus6 = [
  `return encodeURIComponent("a b&c");`,
  `return decodeURIComponent("a%20b");`,
  `return encodeURI("a b&c");`,
  `return decodeURI("a%20b");`,
  `return encodeURIComponent("中文");`,
  `return decodeURIComponent("%E4%B8%AD");`,
];

// ---- URL ----
export const corpus7 = [
  `return new URL("https://a.com/p?x=1").pathname;`,
  `return new URL("https://a.com/p?x=1").hostname;`,
  `return new URL("https://a.com/p?x=1").search;`,
  `return new URL("https://a.com/p?x=1").protocol;`,
  `const u = new URLSearchParams("a=1&b=2"); return u.get("a");`,
  `const u = new URLSearchParams("a=1&b=2"); return u.has("c");`,
  `const u = new URLSearchParams("a=1"); u.append("a","2"); return u.get("a");`,
  `return new URLSearchParams([["a","1"]]).toString();`,
];

// ---- Error ----
export const corpus8 = [
  `return new Error("boom").message;`,
  `return new Error("boom").name;`,
  `return new TypeError("t").name;`,
  `return new RangeError("r").name;`,
  `return new SyntaxError("s").name;`,
  `return new Error("boom") instanceof Error;`,
  `return new TypeError("t") instanceof Error;`,
  `return new Error("boom") instanceof TypeError;`,
  `return Error("no-new").message;`,
  `return TypeError("t").name;`,
];

// ---- Symbol ----
export const corpus9 = [
  `return typeof Symbol("x");`,
  `return Symbol("x").description;`,
  `return Symbol.for("k") === Symbol.for("k");`,
  `return Symbol.keyFor(Symbol.for("k"));`,
  `return typeof Symbol.iterator;`,
  `return typeof Symbol.toStringTag;`,
  `return String(Symbol("x"));`,
];

// ---- 运算 ----
export const corpus10 = [
  `return 7 << 2;`,
  `return -7 >> 1;`,
  `return -7 >>> 1;`,
  `return 5 & 3;`,
  `return 5 | 3;`,
  `return 5 ^ 3;`,
  `return ~5;`,
  `return 7 % 3;`,
  `return -7 % 3;`,
  `return 2 ** 10;`,
  `return 2 ** -1;`,
  `return "5" + 3;`,
  `return 5 + "3";`,
  `return "5" - 3;`,
  `return "5" * "3";`,
  `return "5" / "2";`,
  `return +"42";`,
  `return -"42";`,
  `return +"";`,
  `return +null;`,
  `return -true;`,
  `return 1 < "2";`,
  `return "10" < "9";`,
  `return 0 == false;`,
  `return "" == 0;`,
  `return null == undefined;`,
  `return null == 0;`,
  `return NaN == NaN;`,
  `return NaN === NaN;`,
  `return "5" == 5;`,
  `return "5" === 5;`,
  `return 0 === -0;`,
  `return 0 == -0;`,
  `return 0.1 + 0.2;`,
  `return 0.1 + 0.2 === 0.3;`,
  `return 9007199254740992 + 1;`,
  `return 5e-324;`,
  `return typeof null;`,
  `return typeof undefined;`,
  `return typeof function(){};`,
  `return typeof [];`,
  `return typeof {};`,
  `return typeof NaN;`,
  `return 1 ? "a" : "b";`,
  `return 0 ? "a" : "b";`,
  `return NaN ? "a" : "b";`,
  `return "" ? "a" : "b";`,
  `return "0" ? "a" : "b";`,
  `return [] ? "a" : "b";`,
  `return {} ? "a" : "b";`,
  `return null ?? "d";`,
  `return 0 ?? "d";`,
  `return "" ?? "d";`,
  `return false ?? "d";`,
];

// ---- 解构 ----
export const corpus11 = [
  `const [a,b] = [1,2]; return a+b;`,
  `const [a,,c] = [1,2,3]; return c;`,
  `const [a,...rest] = [1,2,3]; return rest.length;`,
  `const [a=9] = []; return a;`,
  `const [a=9] = [undefined]; return a;`,
  `const [a=9] = [0]; return a;`,
  `const {x,y} = {x:1,y:2}; return x+y;`,
  `const {x:r} = {x:5}; return r;`,
  `const {x=7} = {}; return x;`,
  `const {x:r=7} = {}; return r;`,
  `const {a=1, b=2} = {b:9}; return a*10+b;`,
  `let a=1,b=2; [a,b]=[b,a]; return a*10+b;`,
  `let a=1,b=2; [a,b]=[b,a]; return a;`,
  `let x=5; ({x}={x:9}); return x;`,
  `const [a,b]=["x","y"]; return a+b;`,
];

// ---- 控制流 ----
export const corpus12 = [
  `let s=0; for (const x of [1,2,3]) s+=x; return s;`,
  `let s=0; for (const x of "abc") s+=x.length; return s;`,
  `let s=""; for (const x of new Set([1,2,3])) s+=x; return s;`,
  `let s=""; for (const x of new Map([["a",1]])) s+=x[0]; return s;`,
  `let s=""; for (const x in {a:1,b:2}) s+=x; return s;`,
  `let s=""; for (const x in [9,8]) s+=x; return s;`,
  `let i=0,s=0; while (i<5) {s+=i;i++;} return s;`,
  `let i=0,s=0; do {s+=i;i++;} while (i<5); return s;`,
  `let s=0; for (let i=0;i<5;i++) { if (i===2) continue; s+=i; } return s;`,
  `let s=0; for (let i=0;i<5;i++) { if (i===2) break; s+=i; } return s;`,
  `try { throw new Error("e"); } catch (e) { return "caught"; }`,
  `try { return "ok"; } finally { }`,
  `let r=""; try { r="t"; } catch (e) { r="c"; } finally { r+="f"; } return r;`,
  `let s=0; for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { if (j===1) break; s++; } } return s;`,
  `let s=0; outer: for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { if (j===1) break outer; s++; } } return s;`,
  `let s=0; for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { if (j===1) continue; s++; } } return s;`,
];

// ---- 对象杂项 ----
export const corpus13 = [
  `return Object.fromEntries([["a",1],["b",2]]);`,
  `return Object.hasOwn({a:1},"a");`,
  `return Object.hasOwn({a:1},"b");`,
  `return Object.is(NaN,NaN);`,
  `return Object.is(0,-0);`,
  `return Object.is(1,1);`,
  `const o={}; Object.defineProperty(o,"x",{value:5,writable:false}); return o.x;`,
  `return {a:1}.hasOwnProperty("a");`,
  `return [1,2].hasOwnProperty("length");`,
  `return "abc".hasOwnProperty("length");`,
  `const o = {a:1}; const c = {...o, b:2}; return c.a+c.b;`,
  `const o = {a:1}; const c = Object.assign({}, o, {a:9}); return c.a;`,
  `return Object.keys({a:1,b:2}).join(",");`,
  `return Object.values({a:1,b:2}).join(",");`,
  `return Object.entries({a:1})[0][1];`,
  `return ({a:1}).a ?? "d";`,
  `return ({a:undefined}).a ?? "d";`,
  `return ({}).a?.b;`,
  `return ({a:{b:3}}).a?.b;`,
  `return (null)?.a;`,
  `const o={}; return (o.x ??= 5);`,
  `const o={}; o.x ??= 5; return o.x;`,
  `let n; return (n ??= 5);`,
];

// ---- 数组更杂 ----
export const corpus14 = [
  `const a=[1,2,3]; return a.push(4);`,
  `const a=[1,2,3]; a.push(4); return a.length;`,
  `const a=[1,2,3]; return a.pop();`,
  `const a=[1,2,3]; return a.shift();`,
  `const a=[1,2,3]; return a.unshift(0);`,
  `const a=[1,2,3]; return a.unshift(0,9);`,
  `const a=[1,2,3]; a.unshift(0); return a[0];`,
  `const a=[1,2,3]; return a.indexOf(2);`,
  `const a=[1,2,3]; return a.lastIndexOf(2);`,
  `const a=[1,2,3]; return a.includes(2);`,
  `const a=[1,2,3]; return a.find(x=>x>1);`,
  `const a=[1,2,3]; return a.findIndex(x=>x>1);`,
  `const a=[1,2,3]; return a.filter(x=>x>1);`,
  `const a=[1,2,3]; return a.map(x=>x*2);`,
  `const a=[1,2,3]; return a.slice(1);`,
  `const a=[1,2,3]; return a.slice(-2);`,
  `const a=[1,2,3]; return a.slice(1,2);`,
  `const a=[1,2,3]; return a.concat([4,5]);`,
  `const a=[1,2,3]; a.splice(1,1); return a.length;`,
  `const a=[1,2,3]; a.splice(1,1); return a[1];`,
  `const a=[1,2,3]; return a.splice(1,1);`,
  `const a=[1,2,3]; a.reverse(); return a[0];`,
  `const a=[3,1,2]; a.sort(); return a[0];`,
  `const a=[1,2,3]; a.fill(0); return a[2];`,
  `const a=[1,2,3]; return a.fill(0);`,
  `const a=[1,2,3,4,5]; a.copyWithin(0,3); return a[0];`,
  `const a=[1,2,3]; return a.every(x=>x>0);`,
  `const a=[1,2,3]; return [...a,4];`,
  `return [1,...[2,3],4];`,
  `return Array.isArray([1,2]);`,
  `return Array.isArray("ab");`,
  `return Array.isArray(null);`,
  `return Array.from("abc");`,
  `return Array.from({length:3},(_,i)=>i);`,
  `return new Array(3).length;`,
  `return new Array(3)[0];`,
  `return Array.of(1,2,3);`,
  `return Array(3);`,
  `const a=[]; a[2]=5; return a.length;`,
  `const a=[1,2,3]; a.length=1; return a.length;`,
  `const a=[1,2,3]; a.length=1; return a[0];`,
  `const a=[1,2,3]; a.length=1; return a[1];`,
  `const a=[1,2,3]; a.length=9; return a.length;`,
  `const a=[1,2,3]; a.length=9; return a[8];`,
  `const a=[1,2,3]; delete a[1]; return a.length;`,
  `const a=[1,2,3]; delete a[1]; return a[1];`,
  `const a=[1,2,3]; delete a[1]; return 1 in a;`,
  `return [1,2,3][Symbol.iterator] ? "y" : "n";`,
];

// ---- 字符串杂项 ----
export const corpus15 = [
  `return "abc"[1];`,
  `return "abc".length;`,
  `return "abc"[9];`,
  `return "a${''}b" === "ab";`,
  `const x=5; return "n="+x;`,
  `const x=5; return \`v=\${x}\`;`,
  `const x=5; return \`v=\${x+1}\`;`,
  `const x=[1,2]; return \`\${x}\`;`,
  `return String.raw\`a\\nb\`;`,
  `return "abc".toWellFormed();`,
  `return "abc".isWellFormed();`,
  `return "𠮷".length;`,
  `return [..."𠮷"].length;`,
  `return "a".charCodeAt(0);`,
  `return "abc".charCodeAt(-1);`,
  `return "abc".substring(1);`,
  `return "abc".substr(-2);`,
  `return "abc".padStart(5);`,
  `return "abc".padEnd(5);`,
  `return "aBc".toLocaleLowerCase();`,
  `return "abc".repeat(2.7);`,
  `return "abc".repeat(-1);`,
  `return "a,b".split(",").length;`,
  `return "".split("");`,
  `return "abc".split("");`,
  `return "abc".split(/(b)/);`,
  `return "abc".search(/b/);`,
  `return "abcabc".replace(/b/g, "X");`,
  `return "ab".concat("c","d");`,
  `return "x".at(0);`,
  `return "x".at(-1);`,
  `return "x".at(5);`,
];

// ---- JSON ----
export const corpus16 = [
  `return JSON.parse('{"a":1}');`,
  `return JSON.parse('{"a":1}').a;`,
  `return JSON.parse('[1,2,3]');`,
  `return JSON.parse('"x"');`,
  `return JSON.parse('5');`,
  `return JSON.parse('null');`,
  `return JSON.parse('true');`,
  `return JSON.parse('');`,
  `return JSON.stringify({a:1});`,
  `return JSON.stringify([1,"a",null]);`,
  `return JSON.stringify("x");`,
  `return JSON.stringify(5);`,
  `return JSON.stringify(null);`,
  `return JSON.stringify(undefined);`,
  `return JSON.stringify(NaN);`,
  `return JSON.stringify(Infinity);`,
  `return JSON.stringify({a:undefined});`,
  `return JSON.stringify({a:()=>{}});`,
  `return JSON.stringify([1,undefined,2]);`,
  `return JSON.stringify({a:1}, null, 2);`,
  `return JSON.stringify({b:2,a:1});`,
];

// ---- Math 更多 ----
export const corpus17 = [
  `return Math.trunc(3.7);`,
  `return Math.trunc(-3.7);`,
  `return Math.cbrt(27);`,
  `return Math.expm1(0);`,
  `return Math.log1p(0);`,
  `return Math.log2(8);`,
  `return Math.log10(100);`,
  `return Math.hypot(3,4);`,
  `return Math.fround(1.337);`,
  `return Math.imul(3,4);`,
  `return Math.clz32(1);`,
  `return Math.sinh(0);`,
  `return Math.cosh(0);`,
  `return Math.tanh(0);`,
  `return Math.asinh(0);`,
  `return Math.acosh(1);`,
  `return Math.atanh(0);`,
  `return Math.sign(0);`,
  `return Math.sign(-5);`,
  `return Math.abs(-5);`,
  `return Math.abs(-0);`,
  `return Math.floor(-1.5);`,
  `return Math.ceil(-1.5);`,
  `return Math.round(2.5);`,
  `return Math.round(-2.5);`,
  `return Math.pow(2,10);`,
  `return Math.pow(-2,3);`,
  `return Math.sqrt(-1);`,
  `return Math.min(1,2,3);`,
  `return Math.max(1,2,3);`,
  `return Math.max();`,
  `return Math.min();`,
  `return Math.atan2(1,1);`,
];

// ---- Date 更多 ----
export const corpus18 = [
  `return new Date(0).getTime();`,
  `return new Date(0).getUTCFullYear();`,
  `return new Date(0).getFullYear();`,
  `return new Date(0).toISOString();`,
  `return new Date(0).valueOf();`,
  `return typeof Date.now();`,
  `return new Date(2020,0,15).getDate();`,
  `return new Date("2020-01-15").getUTCDate();`,
  `return new Date("2020-01-15").getUTCMonth();`,
];
