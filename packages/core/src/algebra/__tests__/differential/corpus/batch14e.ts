export const mapSetEdge = [
  "const m = new Map(); m.set('a', 1); return m.get('a')",
  "const m = new Map(); m.set('a', 1); m.set('a', 2); return m.get('a')",
  "const m = new Map([['a',1]]); return m.get('a')",
  "const m = new Map([['a',1]]); return m.has('a')",
  "const m = new Map([['a',1]]); return m.has('z')",
  "const m = new Map([['a',1],['b',2]]); return m.size",
  "const m = new Map([['a',1]]); m.delete('a'); return m.size",
  "const m = new Map([['a',1]]); return m.delete('z')",
  "const m = new Map([['a',1],['b',2]]); m.clear(); return m.size",
  "const m = new Map([['a',1],['b',2]]); return [...m.keys()].join('-')",
  "const m = new Map([['a',1],['b',2]]); return [...m.values()].join('-')",
  "const m = new Map([['a',1],['b',2]]); return [...m.entries()].length",
  "const m = new Map([['a',1],['b',2]]); return [...m].length",
  "const m = new Map([['a',1]]); let s = 0; m.forEach((v,k) => { s += v; }); return s",
  "const s = new Set([1,2,3]); return s.size",
  "const s = new Set([1,2]); return s.has(2)",
  "const s = new Set([1,2]); s.delete(1); return s.size",
  "const s = new Set([1,2]); return s.delete(9)",
  "const s = new Set([1,2,3]); s.clear(); return s.size",
  "const s = new Set([1,2,3]); return [...s.values()].join('-')",
  "const s = new Set([1,2]); return [...s.keys()].join('-')",
  "const s = new Set([1,2]); return [...s.entries()].length",
  "const s = new Set([1,2,3]); let sum = 0; s.forEach(v => { sum += v; }); return sum",
  "const s = new Set([NaN]); return s.has(NaN)",
  "const s = new Set([NaN]); return s.size",
  "const s = new Set(); s.add(1).add(1); return s.size",
  "const m = new Map([['a',1]]); return m.get('z')",
  "const m = new Map(); return m.size",
  "const s = new Set(); return s.size",
  "const s = new Set('abc'); return s.size",
  "const m = new Map(); m.set(NaN, 'x'); return m.has(NaN)",
  "const m = new Map(); m.set(-0, 'z'); return m.get(0)",
  "const s = new Set(); s.add(-0); return s.has(0)",
];
