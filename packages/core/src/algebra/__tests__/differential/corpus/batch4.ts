
// ---- Map 全套 ----
export const corpus1 = [
  `const m = new Map([["a",1]]); return typeof m.forEach;`,
  `const m = new Map([["a",1],["b",2]]); let s=0; m.forEach((v)=>s+=v); return s;`,
  `const m = new Map([["a",1],["b",2]]); let s=0; m.forEach((v,k)=>s+=k.length); return s;`,
  `const m = new Map([["a",1]]); m.forEach((v)=>{ if(v===1) return; }); return "done";`,
  `const m = new Map([["a",1]]); return m.clear === undefined ? "no" : "fn";`,
  `const m = new Map([["a",1]]); m.clear(); return m.size;`,
  `const m = new Map([["a",1]]); const r = m.set("b",2); return r === m;`,
  `const m = new Map([["a",1]]); const r = m.delete("a"); return r;`,
  `const m = new Map([["a",1]]); const r = m.delete("z"); return r;`,
  `const m = new Map(); const r = m.get("k"); return r === undefined;`,
  `const m = new Map([["a",1]]); return typeof m[Symbol.iterator];`,
  `const m = new Map([["a",1]]); let s=0; for (const [k,v] of m) s+=v; return s;`,
  `const m = new Map([["a",1],["b",2]]); return m.size;`,
];

// ---- Set 全套 ----
export const corpus2 = [
  `const s = new Set([1,2]); return typeof s.forEach;`,
  `const s = new Set([1,2,3]); let a=0; s.forEach((v)=>a+=v); return a;`,
  `const s = new Set([1,2,3]); let a=0; s.forEach((v)=>a+=v); return a;`,
  `const s = new Set([1]); const r = s.add(2); return r === s;`,
  `const s = new Set([1]); const r = s.delete(1); return r;`,
  `const s = new Set([1]); const r = s.delete(9); return r;`,
  `const s = new Set([1,2]); s.clear(); return s.size;`,
  `const s = new Set([1,2,3]); let a=0; for (const v of s) a+=v; return a;`,
  `const s = new Set([1,2]); return typeof s[Symbol.iterator];`,
  `const s = new Set([1,2]); return [...s];`,
  `const s = new Set(); s.add(1); s.add(1); return s.size;`,
  `const s = new Set([NaN,NaN]); return s.size;`,
  `const s = new Set([NaN]); return s.has(NaN);`,
];

// ---- for...in ----
export const corpus3 = [
  `let s=""; for (const x in {a:1,b:2}) s+=x; return s;`,
  `let s=""; for (const x in {a:1,b:2,c:3}) s+=x; return s.length;`,
  `let s=""; for (const x in [9,8]) s+=x; return s;`,
  `let s=""; for (const x in [9,8,7]) s+=x; return s;`,
  `let s=""; for (const x in "abc") s+=x; return s;`,
  `let s=0; const o={a:1,b:2}; for (const k in o) s+=o[k]; return s;`,
  `let s=0; const o={a:1,b:2,c:3}; for (const k in o) { if (k==="b") break; s+=o[k]; } return s;`,
  `let s=0; const o={a:1,b:2,c:3}; for (const k in o) { if (k==="b") continue; s+=o[k]; } return s;`,
  `const o={a:1}; let found=false; for (const k in o) { if (k==="a") found=true; } return found;`,
  `let n=0; for (const k in {}) n++; return n;`,
  `let s=""; for (const x in {10:"a",2:"b"}) s+=x; return s;`,
  `let s=""; const o={}; for (const k in o) s+=k; return s.length;`,
];

// ---- break/continue 各循环 ----
export const corpus4 = [
  `let s=0; for (let i=0;i<5;i++) { if (i===2) continue; s+=i; } return s;`,
  `let s=0; for (let i=0;i<5;i++) { if (i===2) break; s+=i; } return s;`,
  `let i=0,s=0; while (i<5) { i++; if (i===3) continue; s+=i; } return s;`,
  `let i=0,s=0; while (i<5) { i++; if (i===3) break; s+=i; } return s;`,
  `let i=0,s=0; do { i++; if (i===3) continue; s+=i; } while (i<5); return s;`,
  `let i=0,s=0; do { i++; if (i===3) break; s+=i; } while (i<5); return s;`,
  `let s=0; for (let i=0;i<5;i++) { for (let j=0;j<3;j++) { if (j===1) break; s++; } } return s;`,
  `let s=0; outer: for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { if (j===1) break outer; s++; } } return s;`,
  `let s=0; outer: for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { if (j===1) continue outer; s++; } } return s;`,
  `let s=0; for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { if (j===1) continue; s++; } } return s;`,
  `let s=0; for (let i=0;i<5;i++) { if (i%2===0) continue; s+=i*10; } return s;`,
  `let i=0,s=0; while (true) { i++; if (i>3) break; if (i===2) continue; s+=i; } return s;`,
  `let s=0; for (let i=0;i<10;i++) { if (i===3) { continue; } s+=i; if (i===5) break; } return s;`,
  `let s=0; for (let i=0;i<3;i++) { for (let j=0;j<3;j++) { for (let k=0;k<3;k++) { if (k===1) continue; s++; } } } return s;`,
];

// ---- 逻辑赋值 ----
export const corpus5 = [
  `const o={}; return (o.x ??= 5);`,
  `const o={}; o.x ??= 5; return o.x;`,
  `const o={}; o.x ??= 5; return o;`,
  `let n; return (n ??= 5);`,
  `let n=undefined; n ??= 5; return n;`,
  `let n=0; n ??= 5; return n;`,
  `let n=null; n ??= 5; return n;`,
  `let n=1; n ||= 5; return n;`,
  `let n=0; n ||= 5; return n;`,
  `let n=""; n ||= "d"; return n;`,
  `let n=1; n &&= 5; return n;`,
  `let n=0; n &&= 5; return n;`,
  `let n="x"; n &&= "y"; return n;`,
  `const o={x:1}; return (o.x ||= 9);`,
  `const o={x:1}; o.x ||= 9; return o.x;`,
  `const o={x:0}; o.x ||= 9; return o.x;`,
  `const o={x:1}; return (o.x &&= 9);`,
  `const o={x:0}; o.x &&= 9; return o.x;`,
  `const o={x:null}; o.x ??= 9; return o.x;`,
  `const o={}; o.y ??= 5; return o.y;`,
  `const o={x:{}}; (o.x.y ??= 5); return o.x.y;`,
  `const o={x:{}}; return (o.x.y ??= 5);`,
  `const a=[undefined]; return (a[0] ??= 7);`,
  `const a=[undefined]; a[0] ??= 7; return a[0];`,
  `const a=[]; a[1] ??= 7; return a[1];`,
  `const a=[1]; a[0] &&= 7; return a[0];`,
  `const a=[0]; a[0] ||= 7; return a[0];`,
  `let n=0; n ||= (()=>{return 8})(); return n;`,
  `let n=0, calls=0; n ||= (calls++, 8); return calls;`,
];

// ---- delete + in ----
export const corpus6 = [
  `const o={a:1}; delete o.a; return "a" in o;`,
  `const o={a:1}; delete o.a; return o.hasOwnProperty("a");`,
  `const o={a:1}; return delete o.a;`,
  `const o={a:1}; return delete o.b;`,
  `const o={a:1}; delete o.a; return o.a;`,
  `const a=[1,2,3]; delete a[1]; return 1 in a;`,
  `const a=[1,2,3]; delete a[1]; return a[1];`,
  `const a=[1,2,3]; delete a[1]; return a.length;`,
  `const a=[1,2,3]; delete a[1]; return 0 in a;`,
  `const a=[1,2,3]; delete a[1]; return 2 in a;`,
  `const a=[1,2,3]; return delete a[1];`,
  `const a=[1,2,3]; return delete a[9];`,
  `const a=[1,2,3]; delete a[9]; return a.length;`,
  `let x=5; return delete x;`,
  `const o={}; return delete o.x;`,
  `const o={a:1}; return "a" in o;`,
  `const o={a:1}; return "b" in o;`,
  `const o={a:1}; return "hasOwnProperty" in o;`,
  `const o={}; return "toString" in o;`,
  `const o=Object.create(null); return "toString" in o;`,
  `const o=Object.create(null); return "x" in o;`,
  `const a=[1]; return "length" in a;`,
  `const a=[1]; return "0" in a;`,
  `const a=[1]; return "1" in a;`,
  `return "x" in "abc";`,
];

// ---- switch ----
export const corpus7 = [
  `let r=""; switch(2){case 1: r="a"; break; case 2: r="b"; break; default: r="c";} return r;`,
  `let r=""; switch(9){case 1: r="a"; break; default: r="c";} return r;`,
  `let r=""; switch(1){case 1: r="a"; case 2: r+="b"; break;} return r;`,
  `let r=""; switch("x"){case "y": r="a"; break; default: r="d";} return r;`,
  `let r=""; switch(1){case 1: r="a"; break;} return r;`,
  `let n=0; switch(2){case 1: n++; break; case 2: n+=10; break;} return n;`,
  `let r=""; switch(1){case 1: r="a"; break; case 2: r="b";} return r;`,
  `let r=""; switch(3){case 1: r="a"; break; case 2: r="b"; break; default: r="d";} return r;`,
  `const f=(x)=>{ switch(x){case 1: return "one"; case 2: return "two"; default: return "many";} }; return f(2);`,
  `let r=""; switch(0){case 0: r="z"; break;} return r;`,
];

// ---- try/catch/finally ----
export const corpus8 = [
  `try { throw new Error("e"); } catch (e) { return "caught"; }`,
  `try { throw new Error("e"); } catch (e) { return e.message; }`,
  `try { throw 42; } catch (e) { return typeof e; }`,
  `let r=""; try { throw 1; } catch (e) { r="c"+e; } return r;`,
  `let r=""; try { r="t"; } catch (e) { r="c"; } return r;`,
  `let r=""; try { r="t"; } finally { r+="f"; } return r;`,
  `let r=""; try { throw 1; } catch (e) { r="c"; } finally { r+="f"; } return r;`,
  `try { return 1; } finally { }`,
  `try { return 1; } finally { 2+2; }`,
  `const f=()=>{ try { return 1; } finally { } }; return f();`,
  `let r=""; try { try { throw 1; } finally { r+="i"; } } catch (e) { r+="o"; } return r;`,
  `let r=""; try { throw 1; } catch (e) { try { throw 2; } catch (e2) { r+="n"; } } return r;`,
  `try { throw new TypeError("t"); } catch (e) { return e.name; }`,
  `let r=""; try { throw 1; } catch (e) { r="c"; } finally { } return r;`,
  `try { } catch (e) { return "c"; } finally { }`,
];

// ---- 逻辑运算符返回值 ----
export const corpus9 = [
  `return "a" || "b";`,
  `return "" || "b";`,
  `return 0 || "b";`,
  `return null || "b";`,
  `return undefined || "b";`,
  `return "a" && "b";`,
  `return "" && "b";`,
  `return 0 && "b";`,
  `return 1 && 0;`,
  `return "x" && 1;`,
  `return NaN || 0;`,
  `return NaN && 0;`,
  `return 0 || false || null;`,
  `return 1 && 2 && 3;`,
];

// ---- 逗号/序列/void ----
export const corpus10 = [
  `return (1,2,3);`,
  `let n=0; return (n++, n+=2, n);`,
  `let n=0; const r=(n++, n+5); return n;`,
  `let n=0; const r=(n++, n+5); return r;`,
  `return void 0;`,
  `return void 5;`,
  `let n=5; void (n=9); return n;`,
];

// ---- 参数默认值/rest ----
export const corpus11 = [
  `const f=(a=5)=>a; return f();`,
  `const f=(a=5)=>a; return f(undefined);`,
  `const f=(a=5)=>a; return f(0);`,
  `const f=(a=5)=>a; return f(null);`,
  `const f=({x=9})=>x; return f({});`,
  `const f=({x=9})=>x; return f({x:1});`,
  `const f=([a,b=2])=>a+b; return f([1]);`,
  `const f=(...rest)=>rest.length; return f(1,2,3);`,
  `const f=(...rest)=>rest.length; return f();`,
  `const f=(a,b=()=>a*2)=>b(); return f(3);`,
  `const f=(a=1,b=a+1)=>b; return f();`,
  `const f=(a=1,b=a+1)=>b; return f(5);`,
  `function g(a=7){ return a; } return g();`,
  `function g(a=7){ return a; } return g(undefined);`,
  `function g(a=7){ return a; } return g(null);`,
];

// ---- 闭包/递归/IIFE ----
export const corpus12 = [
  `let n=0; const f=()=>{ n++; return n; }; f(); f(); return f();`,
  `const mk=(x)=>(y)=>x+y; return mk(1)(2);`,
  `let calls=0; const counter=()=>{ calls++; return calls; }; counter(); return counter();`,
  `const f=(n)=>n<=1?1:n*f(n-1); return f(5);`,
  `const fib=(n)=>n<2?n:fib(n-1)+fib(n-2); return fib(6);`,
  `let acc=0; const sum=(n)=>{ if(n===0) return acc; acc+=n; return sum(n-1); }; return sum(4);`,
  `let x=10; const f=()=>{ let x=20; return ()=>x; }; return f()();`,
  `let x=10; const f=()=>{ let x=20; x++; return x; }; f(); return x;`,
  `return ((a,b)=>a+b)(1,2);`,
  `return (function(n){ return n*2; })(21);`,
  `let v=0; (function(){ v=7; })(); return v;`,
  `const f=(a)=>((b)=>((c)=>a+b+c))(1)(2); return f(3);`,
];

// ---- 类 ----
export const corpus13 = [
  `class A { constructor(){ this.x=1; } getX(){ return this.x; } } const a=new A(); return a.getX();`,
  `class A { constructor(){ this.x=1; } } const a=new A(); return a.x;`,
  `class A { m(){ return 1; } } const a=new A(); return a.m();`,
  `class A { static s(){ return 2; } } return A.s();`,
  `class A {} A.p = 5; return A.p;`,
  `class B extends A {} class A { m(){ return "base"; } } const b=new B(); return b.m();`,
  `class A { m(){ return "base"; } } class B extends A { m(){ return "derived"; } } return new B().m();`,
  `class A { m(){ return "base"; } } class B extends A { n(){ return this.m(); } } return new B().n();`,
  `class A { get v(){ return 42; } } return new A().v;`,
  `class A { get v(){ return 42; } } const a=new A(); a.v=1; return a.v;`,
  `class A { set v(x){ this._v=x*2; } get v(){ return this._v; } } const a=new A(); a.v=5; return a.v;`,
  `class A { constructor(){ this.n=0; } inc(){ this.n++; return this.n; } } const a=new A(); a.inc(); return a.inc();`,
  `class A { constructor(n){ this.n=n; } } return new A(5).n;`,
  `class A { m(){ return "a"; } } return typeof new A().m;`,
  `class A {} return new A() instanceof A;`,
  `class A {} class B extends A {} return new B() instanceof A;`,
  `class A {} class B {} return new B() instanceof A;`,
  `class A { get x(){ return 1; } } return "x" in new A();`,
];

// ---- 对象字面量 getter/setter ----
export const corpus14 = [
  `const o={ get x(){ return 5; } }; return o.x;`,
  `const o={ _x:1, get x(){ return this._x; }, set x(v){ this._x=v*2; } }; o.x=3; return o.x;`,
  `const o={ _x:1, get x(){ return this._x; }, set x(v){ this._x=v*2; } }; o.x=3; return o._x;`,
  `const o={ set x(v){ this.y=v+1; } }; o.x=4; return o.y;`,
  `const o={ get x(){ return 5; } }; o.x=9; return o.x;`,
  `let t=0; const o={ get x(){ t++; return t; } }; const a=o.x; return a;`,
  `let t=0; const o={ get x(){ t++; return t; } }; o.x; o.x; return t;`,
  `const o={ get x(){ return 5; } }; return Object.keys(o).length;`,
  `const o={ get x(){ return 5; } }; return "x" in o;`,
  `const o={ get x(){ return 5; } }; return Object.getOwnPropertyDescriptor ? "d" : "n";`,
];

// ---- 字符串数字边缘 ----
export const corpus15 = [
  `return "abc"[0] + "abc"[1];`,
  `return ""[0];`,
  `return "ab".charAt(0.7);`,
  `return "ab".charAt(1.9);`,
  `return "ab".charAt(NaN);`,
  `return "ab".charAt(Infinity);`,
  `return "abc".indexOf("");`,
  `return "abc".indexOf("", 2);`,
  `return "abc".lastIndexOf("");`,
  `return "abc".includes("");`,
  `return "abc".slice(0,0);`,
  `return "abc".slice(3,1);`,
  `return "abc".slice(-1,-2);`,
  `return "abc".substring(NaN,2);`,
  `return "abc".substr(0);`,
  `return "abc".substr(1);`,
  `return "abc".substr(-1,2);`,
  `return "  ".trim().length;`,
  `return "ab".repeat(0);`,
  `return "ab".replace("a","");`,
  `return "aaa".replace("a","b");`,
  `return "abc".toUpperCase()[0];`,
];

// ---- 数组边缘 ----
export const corpus16 = [
  `return [1,2,3].indexOf(2,2);`,
  `return [1,2,3].indexOf(2,-1);`,
  `return [1,2,1].lastIndexOf(1,-2);`,
  `return [1,2,3].includes(1,-3);`,
  `return [1,2,3].includes(3,-1);`,
  `return [NaN].includes(NaN);`,
  `return [NaN].indexOf(NaN);`,
  `return [1,2,3].slice(0,0);`,
  `return [1,2,3].slice(3);`,
  `return [1,2,3].slice(-1,-2);`,
  `return [1,2,3].concat();`,
  `return [].concat([1]);`,
  `return [1].concat(2);`,
  `return [1,2,3].join();`,
  `return [1,null,undefined].join("-");`,
  `const a=[1,2,3]; a.length=0; return a.length;`,
  `const a=[1,2,3]; a.length=0; return a[0];`,
  `const a=[1,2,3]; return a[a.length-1];`,
  `const a=[]; a[a.length]=5; return a.length;`,
  `const a=[]; a.push(1,2,3); return a[2];`,
  `const a=[3,1,2]; return a.sort().join(",");`,
  `const a=[10,9,8]; a.sort((x,y)=>x-y); return a.join(",");`,
  `const a=["b","a","c"]; return a.sort().join(",");`,
  `const a=[1,2,3]; const b=a.map((x,i)=>x+i); return b[2];`,
  `const a=[1,2,3]; const b=a.filter((x,i)=>i>0); return b.length;`,
  `const a=[1,2,3]; let t=0; a.forEach((x)=>t+=x); return t;`,
  `const a=[1,2,3]; return a.reduce((x,y)=>x+y,"");`,
  `const a=[[1],[2]]; return a.flatMap((x)=>x)[1];`,
  `const a=[[1,2],[3]]; return a.flat(0).length;`,
  `const a=[1,2,3,4,5]; a.copyWithin(-2); return a[3];`,
  `const a=[1,2,3,4,5]; a.copyWithin(0,3,4); return a[1];`,
  `const a=[1,2,3,4,5]; a.copyWithin(0,3,4); return a[4];`,
  `const a=[1,2,3]; a.fill(9,1,2); return a[0];`,
  `const a=[1,2,3]; a.fill(9,1,2); return a[1];`,
  `const a=[1,2,3]; a.fill(9,-1); return a[2];`,
  `const a=[1,2,3]; return a.reverse()[0];`,
  `const a=[1,2,3]; return [...a.reverse()][0];`,
  `const a=[1,2,3]; return a.splice(0,2);`,
  `const a=[1,2,3]; a.splice(0,0,9); return a[0];`,
  `const a=[1,2,3]; return a.shift();`,
  `const a=[]; return a.pop();`,
  `const a=[]; return a.shift();`,
  `const a=[1,2]; return a.unshift(0);`,
];

// ---- Math 更多 ----
export const corpus17 = [
  `return Math.floor("3.7");`,
  `return Math.abs("-5");`,
  `return Math.max(1,"2",3);`,
  `return Math.min();`,
  `return Math.max();`,
  `return Math.sqrt(-1);`,
  `return Math.pow(2,0.5);`,
  `return Math.log(0);`,
  `return Math.log(-1);`,
  `return Math.log10(-1);`,
  `return Math.acos(2);`,
  `return Math.asin(2);`,
  `return Math.atanh(2);`,
  `return Math.exp(0);`,
  `return Math.expm1(1);`,
  `return Math.fround(0.1);`,
  `return Math.imul(-1,8);`,
  `return Math.clz32(0);`,
  `return Math.sign(NaN);`,
  `return Math.sign("5");`,
  `return Math.trunc("3.9");`,
  `return Math.round(0.5);`,
  `return Math.round(-0.5);`,
  `return Math.round(1.5);`,
  `return Math.round(-1.5);`,
  `return Math.round(2.5);`,
  `return Math.floor(1e21);`,
  `return Math.abs(-Infinity);`,
  `return Math.abs(NaN);`,
  `return Math.max(NaN,1);`,
  `return Math.min(NaN,1);`,
  `return Math.pow(NaN,0);`,
  `return Math.pow(1,NaN);`,
  `return Math.hypot();`,
  `return Math.hypot(3);`,
];

// ---- 类型转换边缘 ----
export const corpus18 = [
  `return +"0x10";`,
  `return +"  ";`,
  `return +[];`,
  `return +[5];`,
  `return +[1,2];`,
  `return +{};`,
  `return +true;`,
  `return +false;`,
  `return +undefined;`,
  `return +null;`,
  `return -[];`,
  `return -"5";`,
  `return -"";`,
  `return -null;`,
  `return -undefined;`,
  `return 1-"a";`,
  `return "5"-"3";`,
  `return ""-0;`,
  `return "a"-1;`,
  `return "a"*2;`,
  `return "5"/"2";`,
  `return "5"%"2";`,
  `return "5"**"2";`,
  `return 1+null;`,
  `return 1+true;`,
  `return 1+undefined;`,
  `return 1+NaN;`,
  `return "x"+null;`,
  `return "x"+undefined;`,
  `return "x"+true;`,
  `return "x"+[1];`,
  `return "x"+{a:1};`,
  `return ""+{};`,
  `return 1+{};`,
  `return 1+[2];`,
  `return [1]+[2];`,
  `return []+[];`,
  `return []+{};`,
  `return {}+[];`,
  `return 5..toString();`,
  `return (5).toString();`,
  `return (5).toString(2);`,
  `return (255).toString(16);`,
  `return (-5).toString();`,
  `return (5.5).toFixed();`,
  `return (5.5).toFixed(0);`,
  `return (1.005).toFixed(2);`,
  `return (5.5).toPrecision(2);`,
  `return (1234).toExponential();`,
  `return (5).valueOf();`,
  `return "5"==5;`,
  `return ""==0;`,
  `return " "==0;`,
  `return "a"==0;`,
  `return "5"==true;`,
  `return "1"==true;`,
  `return [1]==1;`,
  `return [1]=="1";`,
  `return []==0;`,
  `return []=="";`,
  `return null==false;`,
  `return undefined==false;`,
  `return NaN==false;`,
];

// ---- 全局对象 ----
export const corpus19 = [
  `return typeof window;`,
  `return typeof process;`,
  `return typeof console;`,
  `return typeof console.log;`,
  `return typeof setTimeout;`,
  `return typeof Promise;`,
  `return typeof Map;`,
  `return typeof WeakMap;`,
  `return typeof WeakSet;`,
  `return typeof Symbol;`,
  `return typeof BigInt;`,
  `return typeof Reflect;`,
  `return typeof Proxy;`,
  `return typeof structuredClone;`,
  `return typeof ArrayBuffer;`,
  `return typeof Int8Array;`,
  `return typeof DataView;`,
  `return typeof JSON;`,
  `return typeof Math;`,
  `return typeof Object;`,
  `return typeof Function;`,
  `return typeof parseInt;`,
  `return typeof RegExp;`,
  `return typeof Date;`,
  `return typeof Intl;`,
  `return typeof URL;`,
  `return typeof URLSearchParams;`,
  `return typeof TextEncoder;`,
  `return typeof atob;`,
  `return typeof btoa;`,
];

// ---- RegExp 更多 ----
export const corpus20 = [
  `return /abc/.test("xabcx");`,
  `return /abc/i.test("ABC");`,
  `return /a.c/.test("abc");`,
  `return /^a/.test("ab");`,
  `return /a$/.test("ba");`,
  `return /a+/.test("aaa");`,
  `return /a*/.test("bbb");`,
  `return /a?/.test("b");`,
  `return /[a-z]/.test("X");`,
  `return /[^a]/.test("a");`,
  `return /\\d/.test("a5");`,
  `return /\\w/.test("_");`,
  `return /\\s/.test(" ");`,
  `return /a|b/.test("c");`,
  `return /(ab)+/.test("abab");`,
  `return /(a)(b)/.exec("ab");`,
  `return /a(b)/.exec("cab");`,
  `return /x/.exec("abc");`,
  `return "abc".match(/b/);`,
  `return "abcabc".match(/b/g);`,
  `return "abcabc".matchAll(/b/g);`,
  `return "abc".search(/b/);`,
  `return "abc".search(/z/);`,
  `return /a/.exec("ba");`,
  `return /a/g.test("a");`,
  `const r=/b/g; r.test("ab"); r.test("ab"); return r.lastIndex;`,
  `const r=/b/g; r.exec("ab"); return r.lastIndex;`,
  `const r=/b/g; r.test("ab"); return r.lastIndex;`,
  `return "a,b".split(/,/);`,
  `return "a1b2".split(/\\d/);`,
  `return "abc".replace(/a(b)c/, "$1");`,
  `return "abc".replace(/a(b)c/, "$1$1");`,
  `return "abc".replace(/(a)/, "x$1y");`,
  `return "2024-01-02".replace(/(\\d+)-(\\d+)-(\\d+)/, "$3/$2/$1");`,
];
