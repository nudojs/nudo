
// ---- String 方法 ----
export const corpus1 = [
  `return "abc".charAt(1);`,
  `return "abc".charAt(9);`,
  `return "abc".charAt(-1);`,
  `return "abc".charCodeAt(0);`,
  `return "abc".charCodeAt(9);`,
  `return "abc".codePointAt(1);`,
  `return "abc".at(1);`,
  `return "abc".at(-1);`,
  `return "abc".at(9);`,
  `return "hello world".indexOf("o");`,
  `return "hello world".indexOf("o", 5);`,
  `return "hello world".indexOf("z");`,
  `return "hello world".lastIndexOf("o");`,
  `return "hello world".lastIndexOf("o", 4);`,
  `return "hello".includes("ell");`,
  `return "hello".includes("ell", 2);`,
  `return "hello".startsWith("he");`,
  `return "hello".endsWith("lo");`,
  `return "hello".slice(1, 3);`,
  `return "hello".slice(-3);`,
  `return "hello".slice(1);`,
  `return "hello".substring(1, 3);`,
  `return "hello".substring(3, 1);`,
  `return "hello".substring(-3);`,
  `return "hello".substr(1, 3);`,
  `return "hello".repeat(3);`,
  `return "hello".repeat(0);`,
  `return "  hi  ".trim();`,
  `return "  hi  ".trimStart();`,
  `return "  hi  ".trimEnd();`,
  `return "HELLO".toLowerCase();`,
  `return "hello".toUpperCase();`,
  `return "abc".padStart(5, "x");`,
  `return "abc".padEnd(5, "xy");`,
  `return "abc".padStart(2, "x");`,
  `return "a,b,c".split(",");`,
  `return "a,b,c".split("");`,
  `return "a,b,c".split();`,
  `return "a,b,c".split(",", 2);`,
  `return "hello".concat(" ", "world");`,
  `return "hello".replace("l", "L");`,
  `return "hello".replaceAll("l", "L");`,
  `return "hello".replace(/l/, "L");`,
  `return "hello".replace(/l/g, "L");`,
  `return "abc123".search("c");`,
  `return "abc123".search(/\\d/);`,
  `return "abc".match("b");`,
  `return "abcabc".match(/b/g);`,
  `return String.fromCharCode(65, 66);`,
  `return String.fromCodePoint(65, 128512);`,
  `return "x".localeCompare("y");`,
  `return "abc".normalize();`,
  `return "a".toString();`,
  `return "abc".valueOf();`,
];

// ---- Math ----
export const corpus2 = [
  `return Math.abs(-5);`,
  `return Math.ceil(5.1);`,
  `return Math.floor(5.9);`,
  `return Math.round(5.5);`,
  `return Math.round(5.4);`,
  `return Math.round(-5.5);`,
  `return Math.trunc(5.9);`,
  `return Math.trunc(-5.9);`,
  `return Math.sign(-5);`,
  `return Math.sign(0);`,
  `return Math.sign(-0);`,
  `return Math.sign("x");`,
  `return Math.sqrt(16);`,
  `return Math.cbrt(27);`,
  `return Math.exp(0);`,
  `return Math.log(1);`,
  `return Math.log2(8);`,
  `return Math.log10(100);`,
  `return Math.pow(2, 10);`,
  `return Math.min(3, 1, 2);`,
  `return Math.max(3, 1, 2);`,
  `return Math.hypot(3, 4);`,
  `return Math.clz32(1);`,
  `return Math.imul(3, 4);`,
  `return Math.fround(1.5);`,
  `return Math.sin(0);`,
  `return Math.cos(0);`,
  `return Math.tan(0);`,
  `return Math.asin(0);`,
  `return Math.acos(1);`,
  `return Math.atan(0);`,
  `return Math.atan2(1, 2);`,
  `return Math.sinh(0);`,
  `return Math.cosh(0);`,
  `return Math.tanh(0);`,
  `return Math.asinh(0);`,
  `return Math.acosh(1);`,
  `return Math.atanh(0);`,
  `return Math.expm1(0);`,
  `return Math.log1p(0);`,
  `return Math.log2(0);`,
  `return Math.floor(Math.PI);`,
];

// ---- Number ----
export const corpus3 = [
  `return (5).toFixed(2);`,
  `return (5.5).toFixed();`,
  `return (5.567).toFixed(2);`,
  `return (123).toExponential(2);`,
  `return (5.567).toPrecision(2);`,
  `return (5).toPrecision(1);`,
  `return (255).toString(16);`,
  `return (5).toString();`,
  `return Number.isSafeInteger(5);`,
  `return Number.isSafeInteger(2 ** 53);`,
  `return Number.parseFloat("3.14x");`,
  `return Number.parseInt("42px");`,
  `return Number.parseInt("42px", 10);`,
  `return Number("0x10");`,
  `return Number("42");`,
  `return Number("");`,
  `return Number("abc");`,
  `return Number(true);`,
  `return Number(null);`,
  `return (5).valueOf();`,
];

// ---- Object 静态 ----
export const corpus4 = [
  `return Object.keys({a: 1, b: 2});`,
  `return Object.values({a: 1, b: 2});`,
  `return Object.entries({a: 1, b: 2});`,
  `return Object.fromEntries([["a", 1], ["b", 2]]);`,
  `return Object.fromEntries(new Map([["a", 1]]));`,
  `return Object.assign({}, {a: 1});`,
  `return Object.assign({x: 0}, {a: 1});`,
  `return Object.hasOwn({a: 1}, "a");`,
  `return Object.hasOwn({a: 1}, "b");`,
  `return Object.is(0, -0);`,
  `return Object.is(NaN, NaN);`,
  `return Object.is(1, 1);`,
  `return Object.is("a", "a");`,
  `return Object.getOwnPropertyNames([1, 2]);`,
  `return Object.keys([1, 2]);`,
  `return Object.getPrototypeOf({}) === Object.prototype;`,
  `return Object.getPrototypeOf(Object.create(null)) === null;`,
  `let o = {}; Object.defineProperty(o, "p", {value: 42, enumerable: true}); return o.p;`,
  `let o = {a: 1}; Object.freeze(o); o.a = 2; return o.a;`,
  `let o = {a: 1}; Object.freeze(o); return Object.isFrozen(o);`,
  `let o = {a: 1}; Object.seal(o); o.a = 2; return o.a;`,
  `let o = {}; return Object.keys(o);`,
];

// ---- JSON ----
export const corpus5 = [
  `return JSON.stringify(42);`,
  `return JSON.stringify("x");`,
  `return JSON.stringify(true);`,
  `return JSON.stringify(null);`,
  `return JSON.stringify([1, 2, 3]);`,
  `return JSON.stringify({a: 1, b: [2]});`,
  `return JSON.stringify(undefined);`,
  `return JSON.stringify(NaN);`,
  `return JSON.parse("42");`,
  `return JSON.parse('"x"');`,
  `return JSON.parse("true");`,
  `return JSON.parse("null");`,
  `return JSON.parse("[1,2]").length;`,
  `return JSON.parse('{"a":1}').a;`,
];

// ---- Date ----
export const corpus6 = [
  `return new Date(0).getTime();`,
  `return new Date(86400000).getTime();`,
  `return new Date(0).getUTCFullYear();`,
  `return new Date(0).getUTCMonth();`,
  `return new Date(0).getUTCDate();`,
  `return new Date(0).getUTCDay();`,
  `return new Date(0).getUTCHours();`,
  `return new Date(0).getUTCMinutes();`,
  `return new Date(0).getUTCSeconds();`,
  `return new Date(0).getUTCMilliseconds();`,
  `return new Date(2020, 0, 15).getFullYear();`,
  `return new Date(2020, 0, 15).getMonth();`,
  `return new Date(2020, 0, 15).getDate();`,
  `return new Date(2020, 0, 15).getDay();`,
  `return new Date("2020-01-15T00:00:00Z").getTime();`,
  `return new Date(2020, 5, 1, 12, 30, 45, 500).getTime();`,
  `return Date.UTC(2020, 0, 15);`,
  `return new Date(1000).valueOf();`,
  `return new Date(0) instanceof Date;`,
];

// ---- Array 其它 ----
export const corpus7 = [
  `return [1, 2, 3].at(1);`,
  `return [1, 2, 3].at(-1);`,
  `return [1, 2, 3].at(5);`,
  `return [1, 2, 3].find(x => x > 1);`,
  `return [1, 2, 3].find(x => x > 9);`,
  `return [1, 2, 3].findIndex(x => x > 1);`,
  `return [1, 2, 3].findIndex(x => x > 9);`,
  `return [1, 2, 3].findLast(x => x > 1);`,
  `return [1, 2, 3].findLastIndex(x => x > 1);`,
  `return [1, 2, 3].some(x => x > 2);`,
  `return [1, 2, 3].some(x => x > 9);`,
  `return [1, 2, 3].every(x => x > 0);`,
  `return [1, 2, 3].every(x => x > 1);`,
  `return [1, 2, 3].indexOf(2);`,
  `return [1, 2, 3].indexOf(2, 2);`,
  `return [1, 2, 3].indexOf(9);`,
  `return [1, 2, 3].lastIndexOf(2);`,
  `return [1, 2, 3].includes(2);`,
  `return [1, 2, 3].includes(2, 2);`,
  `return [1, 2, 3].join("-");`,
  `return [1, 2, 3].join();`,
  `return [].join("-");`,
  `return [1, [2, 3], [4]].flat();`,
  `return [1, [2, [3]]].flat(2);`,
  `return [1, [2, [3]]].flat();`,
  `return [1, 2, 3].flatMap(x => [x, x * 2]);`,
  `return [1, 2, 3].toReversed();`,
  `return [3, 1, 2].toSorted();`,
  `return [1, 2, 3].with(1, 9);`,
  `return [1, 2, 3].toSpliced(1, 1);`,
  `return [1, 2, 3].toSpliced(1, 0, 9);`,
  `return [1, 2, 3].concat([4, 5]);`,
  `return [1, 2, 3].concat(4, [5]);`,
  `return Array.from("abc");`,
  `return Array.from([1, 2, 3], x => x * 2);`,
  `return Array.of(1, 2, 3);`,
  `return Array.isArray([1]);`,
  `return Array.isArray("x");`,
  `return Array(3).length;`,
  `return Array(3).fill(0);`,
  `let a = [1, 2, 3]; return a.pop();`,
  `let a = [1, 2, 3]; a.pop(); return a;`,
  `let a = [1, 2]; a.push(3); return a;`,
  `let a = [1, 2, 3]; return a.shift();`,
  `let a = [1, 2]; a.unshift(0); return a;`,
  `let a = [1, 2, 3]; a.reverse(); return a;`,
  `let a = [3, 1, 2]; a.sort(); return a;`,
  `let a = [3, 1, 2]; return a.sort((x, y) => x - y);`,
  `let a = [1, 2, 3]; return a.splice(1, 1);`,
  `let a = [1, 2, 3]; a.splice(1, 1, 9); return a;`,
  `let a = [1, 2, 3]; a.splice(1, 0, 9); return a;`,
  `let a = [1, 2, 3, 4]; a.copyWithin(0, 2); return a;`,
  `let a = [1, 2, 3, 4]; a.copyWithin(0, 2, 3); return a;`,
  `let a = [1, 2, 3, 4, 5]; a.copyWithin(3, 0); return a;`,
  `let a = [1, 2, 3, 4, 5]; a.copyWithin(0, 3, 5); return a;`,
  `let a = [1, 2, 3, 4]; a.fill(0); return a;`,
  `let a = [1, 2, 3, 4]; a.fill(0, 1); return a;`,
  `let a = [1, 2, 3, 4]; a.fill(0, 1, 3); return a;`,
  `let a = [1, 2, 3, 4]; a.fill(0, -2); return a;`,
  `let a = [1, 2, 3, 4]; a.fill(0, -3, -1); return a;`,
  `let a = [1, 2, 3]; let s = 0; a.forEach(x => { s += x; }); return s;`,
  `return [1, 2, 3].reduce((a, b) => a + b);`,
  `return [1, 2, 3].reduce((a, b) => a + b, 10);`,
  `return [1, 2, 3].reduceRight((a, b) => a - b);`,
  `return [1, 2, 3].reduce((a, b) => a + b, "");`,
  `return [1, 2, 3].map(x => x * 2);`,
  `return [1, 2, 3].filter(x => x > 1);`,
];

// ---- RegExp ----
export const corpus8 = [
  `return /b/.test("abc");`,
  `return /z/.test("abc");`,
  `return /b/g.test("abc");`,
  `return /b/i.test("ABC");`,
  `return /^a/.test("abc");`,
  `return /c$/.test("abc");`,
  `return /b.c/.test("abc");`,
  `return /b+/.test("abbbc");`,
  `return /b*/.test("ac");`,
  `return /\\d/.test("a1");`,
  `return /\\w/.test("a1");`,
  `return /\\s/.test("a b");`,
  `return /[a-c]/.test("b");`,
  `return /[^a-c]/.test("b");`,
  `return /x|y/.test("y");`,
  `return /(?=b)/.test("abc");`,
  `return /(?!b)/.test("ac");`,
  `return /(ab)+/.test("abab");`,
  `return /\\d{2}/.test("12");`,
  `return /\\d{2}/.test("1");`,
  `return /b/.exec("abc")[0];`,
  `return /z/.exec("abc");`,
  `return /(a)(b)/.exec("ab")[1];`,
  `return /(a)(b)/.exec("ab")[2];`,
  `return /(a)(b)/.exec("ab").index;`,
  `return /b/g.exec("abc");`,
  `return /b/g.exec("abc").index;`,
  `let r = /b/g; r.exec("abc"); return r.lastIndex;`,
  `let r = /b/g; r.test("abc"); return r.lastIndex;`,
  `return "abc".replace(/b/g, "X");`,
  `return "abc".split(/b/);`,
  `return "abc".search(/c/);`,
  `return /abc/.source;`,
  `return /abc/gi.flags;`,
  `return /abc/g.global;`,
  `return /abc/i.ignoreCase;`,
  `return /abc/m.multiline;`,
  `return /abc/.lastIndex;`,
];
