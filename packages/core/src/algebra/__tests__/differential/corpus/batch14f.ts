export const regexEdge4 = [
  "const r = /a/g; let c = 0; while (r.exec('aba')) c++; return c",
  "const r = /a/g; r.exec('aba'); return r.lastIndex",
  "const r = /a/g; r.exec('aaa'); r.exec('aaa'); return r.lastIndex",
  "const r = /a/; return r.test('ba')",
  "const r = /a/g; r.test('aba'); return r.lastIndex",
  "return 'abc'.replace('b', 'X')",
  "return 'abc'.replace(/b/, 'X')",
  "return 'abc'.replace(/b/g, 'X')",
  "return 'a1b2'.replace(/\\d/, m => '<' + m + '>')",
  "return 'a1b2'.replace(/\\d/g, m => m + m)",
  "return 'abc'.replace(/b/, (m, i) => String(i))",
  "return 'abc'.replace(/b/g, (m, i) => String(i))",
  "return 'abc'.replace('b', (m, i) => String(i))",
  "return 'abc'.replace(/z/, 'X')",
  "return 'a-b-c'.split('-').join('+')",
  "return 'a-b-c'.split(/-/).length",
  "return 'abc'.split('').join('-')",
  "return 'abc'.split('', 2).join('-')",
  "return 'a,b;c'.split(/[,;]/).length",
  "return /a/.source",
  "return /a/gi.flags",
  "return /ab+c/.test('abbc')",
  "return /\\d+/.test('abc123')",
  "return /^abc$/.test('abc')",
  "return /[a-z]+/i.test('ABC')",
  "return /\\bword\\b/.test('a word here')",
  "return 'abc'.match(/b/).length",
  "return 'abca'.match(/a/g).length",
  "return 'abc'.match(/z/g)",
  "return 'xyz'.replace(/y/, '<$&>')",
  "return 'abc'.replace(/b/, '$1')",
  "return 'abc'.replace(/(b)/, '[$1]')",
  "return 'abc'.replace(/(b)(c)/, '$2$1')",
  "const r = /a/g; return r.global",
  "const r = /a/i; return r.ignoreCase",
  "const r = /a/m; return r.multiline",
  "const r = /a/s; return r.dotAll",
  "const r = /a/y; return r.sticky",
  "const r = /a/u; return r.unicode",
  "return new RegExp('a', 'g').test('ba')",
  "let r = ''; try { new RegExp('['); } catch (e) { r = 'threw'; } return r",
  "let r = ''; try { new RegExp('a', 'z'); } catch (e) { r = 'threw'; } return r",
  "return 'a.b'.search(/a\\.b/)",
  "return 'ab'.search(/a.b/)",
  "const r = /(a)(b)/; const m = r.exec('ab'); return m[1] + m[2]",
  "const r = /(?<x>a)/; const m = r.exec('a'); return m.groups.x",
  "const r = /(?:ab)/; const m = r.exec('ab'); return m[0]",
  "const r = /a*/; const m = r.exec('bbb'); return m[0]",
  "const r = /x/; return r.exec('abc') === null",
  "let n = 0; 'a1b2c3'.replace(/\\d/g, () => { n++; return 'X'; }); return n",
];
